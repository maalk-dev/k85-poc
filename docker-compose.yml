services:
  postgres:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: local
      POSTGRES_PASSWORD: local
      POSTGRES_DB: poc_local
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
